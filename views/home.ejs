<%- include("partials/header"); -%>

<div class="card">
  <div class="card-body">
    <h1>Home</h1>
    <p>This is the home page. Click <strong>New Entry</strong> in the navigation
      bar to create a new journal entry. All entries are saved in a cloud-based
      database (MongoDB Atlas), and their title and content will be encrypted.
      Currently you can't edit any of your entries,
      but you can always delete them by clicking on the <strong>Delete</strong>
      button below the full-text version. You can reach the full text of an entry by
      clicking on <strong>Read More.</strong>
    </p>
  </div>
</div>

<%  entries.forEach((entry)=>{ %>
<div class="card">
  <div class="card-body">
    <h2><%=entry.title%></h2>
    <p class="card-subtitle mb-2 text-muted"><%=entry.day%></p>
    <p>
      <%=entry.content.substring(0, 100) + " ..."%>
      <a href="/entries/<%=entry._id%>">Read More</a>
    </p>
  </div>
</div>
<% }) %>

<%- include("partials/footer"); -%>
